language: c++
compiler: gcc
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5
      - gcc-5
      - libboost-program-options-dev

install:
  export CXX="g++-5" CC="gcc-5"

script: ./compile_gcc

